# [Claude Code 작업 지시서] 브라질/인도법인 CKD 컨테이너 수량 산출 자동화

- 기획자: 김은수
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 엑셀 기반 계산 엔진(적재 조합 최적화) + 결과 시각화
- 관련 게시물: 같은 기획자가 Day01 프로젝트 기획에 "컨테이너 수량 예측 및 글로벌 컨테이너/Roro 운임 변동에 따른 원가 절감 효과 시각화 코딩"을 올린 바 있음 (확장 방향 참고)

---

## 0. Claude Code에게 — 작업 방식

1. 핵심은 **계산 로직**이다. UI보다 "생산계획 → 모델별 CKD 박스 수량 → 40ft 컨테이너 적재 조합 → 필요 컨테이너 수"의 산출 규칙을 먼저 정확히 구현하고 테스트한다.
2. 실제 데이터는 모두 엑셀로 다운로드 가능하며 **사외 업로드가 허용**되어 있다(보안 제약 낮음). 단 이 시점에는 파일이 없으므로 4번 스키마로 샘플을 만들어 개발한다.
3. 9번 질문 중 **박스 규격·컨테이너 적재 규칙**은 결과의 정확도를 좌우하므로 반드시 확인한다. 답이 없으면 5번 가정으로 진행하되, 규칙을 **설정 파일로 분리**해 나중에 쉽게 바꿀 수 있게 한다.
4. 부록의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

브라질·인도 법인의 18개월 생산계획(엑셀)과 모델별 CKD 박스 수량, 40ft 컨테이너 적재 규칙(적재율 약 70%)을 입력받아 **월별 필요 컨테이너 수량을 자동 산출**해 물류팀에 전달할 수 있는 도구를 만든다.

## 2. 배경 및 현재 업무 상황

- 브라질·인도 법인이 사내 시스템에 향후 18개월 생산계획을 등록하고, 내자재·외자재 소요량, 선적 요청월 등이 SAP에 등록되어 엑셀로 다운로드 가능.
- CKD 선적 담당자가 **과거 실적 감각으로** 컨테이너 수량을 예측해 물류팀에 전달 → 차월 컨테이너 확보.
- 문제: 과다 예측 시 **미사용 컨테이너 페널티**, 과소 예측 시 **선적 불가 → 법인 매출 실패**.

## 3. 요구 기능

### 3.1 입력
- 법인별(브라질/인도) 생산계획 엑셀 업로드: 모델 × 월별 생산 대수.
- 모델별 CKD 박스 구성 마스터: 모델 1대당 박스 종류별 수량(예: 모델 A = 박스 X 2개, 박스 Y 1개, 박스 Z 3개).
- 박스 규격 마스터: 박스 종류별 치수/부피/중량, 적재 가능 여부·적층 제한.
- 컨테이너 규격: 40ft(내부 치수·최대 적재 부피/중량), 목표 적재율(기본 70%).
- 선적 리드타임: 생산월 대비 선적 요청월 오프셋(법인별).

### 3.2 계산
- 월별·법인별 필요 박스 수량 = Σ(모델 생산 대수 × 모델별 박스 구성).
- 박스 조합에 따른 컨테이너 소요량 산출:
  - 1차(단순): 총 부피 ÷ (컨테이너 부피 × 적재율) 올림.
  - 2차(조합): 40ft에 들어갈 수 있는 박스 종류가 한정되어 있으므로, **허용 조합 규칙**(어떤 박스끼리 같이 실을 수 있는지, 한 컨테이너당 최대 몇 개)을 반영한 bin-packing 근사(First-Fit Decreasing 등).
- 결과: 월별 컨테이너 수(올림), 예상 적재율, 남는 박스(이월) 처리.
- 시나리오: 적재율 65/70/75% 변경 시 컨테이너 수 비교.

### 3.3 출력·시각화
- 법인별 × 월별 컨테이너 소요 표 + 차트(18개월).
- 박스 종류별 물량, 컨테이너별 적재 구성 내역(검증용).
- 물류팀 전달용 엑셀 내보내기.
- (확장) 과거 실적 입력 시 예측 vs 실적 비교, 컨테이너/RoRo 운임 단가 입력 시 원가·페널티 절감 효과 시각화.

## 4. 데이터 스키마 (샘플 데이터 생성 지침)

**생산계획 (`plan_{법인}.xlsx`)**: 법인, 모델명, 2026-09 … 2028-02 (18개월 컬럼, 대수)

**모델-박스 구성 (`model_box.xlsx`)**: 모델명, 박스코드, 대당 박스 수량

**박스 마스터 (`box_master.xlsx`)**: 박스코드, 길이(mm), 폭(mm), 높이(mm), 부피(CBM), 중량(kg), 적층 가능 단수, 혼적 그룹

**컨테이너 규격 (`container.json`)**: 타입(40ft/40HC), 내부 치수, 최대 CBM, 최대 중량, 목표 적재율 0.70

샘플: 모델 6개(법인당 3개), 박스 종류 5~8개, 18개월 계획.

## 5. 제약사항 및 가정

- 적재율 기본값 70%는 설정으로 변경 가능.
- 박스 혼적 규칙을 확인하기 전까지는 "같은 혼적 그룹끼리만 한 컨테이너" + "부피·중량 한도" 규칙으로 가정.
- 컨테이너는 40ft 단일 타입 가정(40HC 등은 옵션).
- 로컬 실행 가능한 형태 우선. 엑셀 입출력은 필수.

## 6. 권장 기술 스택 (제안)

- 옵션 A: Python (pandas + openpyxl) 계산 엔진 + Streamlit UI(업로드·차트·다운로드). 계산 검증이 쉬움.
- 옵션 B: 단일 HTML + JS (SheetJS + Chart.js) — 설치 없이 실행.
- 계산 로직은 UI와 분리된 모듈 + 단위 테스트(소규모 수작업 계산과 대조).

## 7. 산출물

1. 샘플 엑셀 4종
2. 계산 엔진 모듈 + 테스트
3. UI(업로드 → 산출 → 차트 → 엑셀 내보내기)
4. 설정 파일(컨테이너 규격, 적재율, 혼적 규칙)
5. README(산출 규칙 설명, 실제 엑셀 컬럼 맞추는 방법)

## 8. 구현 단계

- Phase 1: 샘플 데이터 + 박스 수량 집계 + 단순 부피 기반 컨테이너 수 산출 + 테스트
- Phase 2: 혼적 규칙·조합 기반 bin-packing 산출 + 컨테이너별 적재 내역
- Phase 3: UI + 차트 + 시나리오(적재율 변경) + 엑셀 내보내기
- Phase 4: (확장) 실적 비교·운임/페널티 효과 시각화, README

## 9. 확인 필요 사항 (작업 전 질문)

1. 박스 종류 수와 각 박스의 치수/부피/중량? 40ft 컨테이너에 **함께 실을 수 있는 박스 조합 규칙**은?
2. 적재율 70%는 부피 기준인가 중량 기준인가?
3. 생산계획 엑셀의 실제 컬럼 구조(모델/월 배치)와 선적 요청월과 생산월의 관계(리드타임)?
4. 남는 박스는 다음 달로 이월하는가, 해당 월에 컨테이너를 추가 확보하는가?
5. 40ft 외 컨테이너 타입(40HC, 20ft)도 사용하는가?
6. 과거 컨테이너 실적 데이터가 있는가(예측 정확도 검증용)?

---

## 부록. 기획서 원문 (Padlet 게시 내용 그대로)

브라질/인도법인 CKD 컨테이너 수량 산출 자동화 구축

현재 상황: 브라질법인과 인도법인에서 차18개월 생산계획을 사내 시스템에 등록을 하고, 내자재 및 외자재 소요량과 선적 요청월 등 모든 자재 선적 관련 정보가 SAP에 등록되어 있고 excel로 다운 받을 수 있음. CKD 선적 담당자가 과거 컨테이너 선적 실적을 바탕으로 컨테이너 수량을 예측해서 물류팀에 전달하여 차월에 대한 컨테이너 수량을 확보하는데, 수량 예측 실패에 따른 과다 컨테이너 선적 확보로 인한 페널티 발생 및 컨테이너 부족에 따른 법인 매출 실패가 발생함.

자동화 계획: 브라질법인과 인도법인에서 판매하는 모델에 대한 생산계획을 excel로 전부 받을 수 있고, 각 모델에 따른 CKD 박스 수량도 지정되어 있으며, 40ft 컨테이너에 들어갈 수 있는 CKD박스 종류가 한정되어 있으니 CKD 박스 조합에 따라서 컨테이너 수량이 산출되어야 함. 통상적으로 컨테이너 적재률 70% 수준으로 컨테이너 소요량이 산출되어야 함.

*컨테이너 산출을 위한 data는 excel로 전부 다운 받아서 사외 인터넷으로 업로딩 가능함.
